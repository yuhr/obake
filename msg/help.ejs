
  👻  obake v<%- obake.version %>

  usage: obake [<option>] [<commands>...] [--] [<arguments>...]
  local config:<%- config.exists ? '' : ' not found' %>
<%
    if (config.exists) {
-%>
    path: <%- config.path %>
    version: <%- config.version %>
<%
    }
-%>

  commands:
<%
    for (const line of lines.commands) {
-%>
    <%- line %>
<%
    }
-%>

  options:
<%
    for (const line of lines.options) {
-%>
    <%- line %>
<%
    }
-%>
